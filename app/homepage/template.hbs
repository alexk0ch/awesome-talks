<div class="row intro">
  {{#each topic in model}}
    <div class="col-md-4 col-lg-4 text-center">
      <h1>{{topic.title}}</h1>
      <img class="img-responsive" src="{{topic.url}}" alt="">
    </div>
  {{/each}}
</div>
{{outlet}}